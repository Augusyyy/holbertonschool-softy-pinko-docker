# Use the latest Ubuntu base image
FROM ubuntu:latest

# Update and upgrade the package lists
RUN apt-get update && apt-get upgrade -y

# Command to echo "Hello, World!" when the container runs
CMD echo "Hello, World!"